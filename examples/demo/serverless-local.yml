service: demo

plugins:
  - ../../ # serverless-core-hooks
  - ./     # demo

provider:
  name: aws
  runtime: nodejs12.x
  stage: ${opt:stage, env:stage, 'dev'}
  region: ${opt:region, env:region, 'us-east-1'}

custom:
  coreHooks:
    objects:
      # - cli
      - pluginManager
      - serverless
      # - service
      - utils
      # - variables
      # - yamlParser
